<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cook.simpleservices">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".ui.MainActivity">

        </activity>

        <activity android:name=".ui.broadcastreceiver.MainBroadcastActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".service.not_sticky.SimpleServiceNotSticky" />
        <service android:name=".service.sticky.SimpleServiceSticky" />
        <service android:name=".service.redeliver_intent.SimpleServiceRedeliverIntent" />

        <service android:name=".service.broadcast.ServiceBroadcastExample"/>

    </application>

</manifest>
